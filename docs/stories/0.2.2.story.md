# Story 0.2.2: Event Attendees Module

**Story ID:** 0.2.2
**Epic:** EPIC-0 (Foundation & Infrastructure)
**Status:** Draft
**Created:** 2026-03-03
**Owner:** @dev (Dex)
**Story Points:** 5
**Sprint:** W1-2 (Wave 1)

---

## 📖 User Story

**As a** system architect
**I want to** have an attendees/members module for events
**So that** Story 1.2 can select and send proposals to event members

---

## ✅ Acceptance Criteria

- [ ] Event attendees stored in database
- [ ] API endpoint `GET /api/events/:eventId/attendees` returns list of members
- [ ] API supports filtering/searching attendees by name or email
- [ ] API includes pagination (for large events)
- [ ] `src/lib/attendees.ts` service created for attendee operations
- [ ] RLS policies ensure admins can only see their event attendees
- [ ] Database schema for attendees/members defined and migrated
- [ ] All tests passing (API, service, RLS)
- [ ] Ready for integration in Story 1.2 (RecipientSelector)

---

## 📋 Technical Details

### Database Schema

**Table: event_attendees** (NEW)
```sql
- id (UUID, PK)
- event_id (UUID, FK → events.id, ON DELETE CASCADE)
- user_id (UUID, FK → auth.users.id, ON DELETE CASCADE)
- email (VARCHAR 255, NOT NULL)
- name (VARCHAR 255, NOT NULL)
- role (ENUM: admin, member, guest, DEFAULT member)
- invited_at (TIMESTAMP, DEFAULT now())
- joined_at (TIMESTAMP, NULL)
- status (ENUM: pending, accepted, declined, DEFAULT pending)
- created_at (TIMESTAMP, DEFAULT now())
- updated_at (TIMESTAMP, DEFAULT now())

UNIQUE (event_id, user_id)
UNIQUE (event_id, email)
```

### RLS Policies

**event_attendees table:**
- Admin of event can SELECT/INSERT/UPDATE attendees for their event
- Policy: `(SELECT EXISTS(SELECT 1 FROM events WHERE id=event_id AND admin_id=auth.uid()))`
- Attendees can SELECT their own record
- Policy: `(user_id = auth.uid() OR (event_id in (SELECT id FROM events WHERE admin_id = auth.uid())))`

### Triggers

**event_attendees:**
- `trigger_updated_at_event_attendees` — Auto-update `updated_at` on row change

---

## 🎯 Implementation Steps

### Phase 1: Database Schema

#### 0.2.2.1 Create Database Migration ✅

**Status:** PENDING

**File:** `src/db/migrations/001-create-event-attendees.sql`

**Content:**
- Create `event_attendees` table
- Add indexes on event_id, user_id, email
- Create RLS policies
- Create triggers for updated_at

**Verify:**
- Table created
- Constraints in place
- RLS policies active

---

#### 0.2.2.2 Update TypeScript Types ✅

**Status:** PENDING

**File:** `src/types/attendee.ts`

**Types:**
```typescript
- EventAttendee
- AttendeeRole ('admin' | 'member' | 'guest')
- AttendeeStatus ('pending' | 'accepted' | 'declined')
- AttendeeListQuery (pagination, search, filter)
- AttendeeListResponse
```

---

### Phase 2: Backend API

#### 0.2.2.3 Create Attendees Service ✅

**Status:** PENDING

**File:** `src/lib/attendees.ts`

**Methods:**
```typescript
- getEventAttendees(eventId, query): Promise<AttendeeListResponse>
- getAttendee(eventId, userId): Promise<EventAttendee>
- addAttendee(eventId, email, name): Promise<EventAttendee>
- removeAttendee(eventId, userId): Promise<boolean>
- searchAttendees(eventId, searchTerm): Promise<EventAttendee[]>
```

**Features:**
- Pagination (20 per page default)
- Search by name or email
- Filter by role or status
- RLS enforcement (only event admin sees list)

---

#### 0.2.2.4 Create API Endpoint: GET /api/events/:eventId/attendees ✅

**Status:** PENDING

**Endpoint:** `GET /api/events/:eventId/attendees`

**Query Parameters:**
```
- page=1 (pagination)
- limit=20 (items per page)
- search=name|email (search term)
- role=admin|member|guest (filter by role)
- status=pending|accepted|declined (filter by status)
```

**Response (200 OK):**
```json
{
  "event_id": "uuid",
  "attendees": [
    {
      "user_id": "uuid",
      "email": "member@example.com",
      "name": "John Smith",
      "role": "member",
      "status": "accepted",
      "joined_at": "2026-03-01T10:00:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 45,
    "pages": 3
  }
}
```

**Error Handling:**
- 400: Invalid query parameters
- 401: Unauthorized (not authenticated)
- 403: Forbidden (not admin of event)
- 404: Event not found

---

#### 0.2.2.5 Create API Endpoint: GET /api/events/:eventId/attendees/:userId ✅

**Status:** PENDING

**Endpoint:** `GET /api/events/:eventId/attendees/:userId`

**Response (200 OK):**
```json
{
  "user_id": "uuid",
  "email": "member@example.com",
  "name": "John Smith",
  "role": "member",
  "status": "accepted",
  "joined_at": "2026-03-01T10:00:00Z"
}
```

---

#### 0.2.2.6 Create API Endpoint: POST /api/events/:eventId/attendees ✅

**Status:** PENDING

**Endpoint:** `POST /api/events/:eventId/attendees`

**Request Body:**
```json
{
  "email": "newmember@example.com",
  "name": "New Member",
  "role": "member"
}
```

**Response (201 Created):**
```json
{
  "user_id": "uuid",
  "email": "newmember@example.com",
  "name": "New Member",
  "role": "member",
  "status": "pending"
}
```

**Error Handling:**
- 400: Invalid email or missing fields
- 403: Not admin of event
- 409: Attendee already exists

---

### Phase 3: Frontend (Optional for MVP)

#### 0.2.2.7 Create Attendees List Component (Optional)

**Status:** PENDING / DEFERRED

This can be added in Story 1.2 when needed for RecipientSelector.

---

### Phase 4: Testing

#### 0.2.2.8 Unit Tests ✅

**Status:** PENDING

**Tests:**
- ✅ getEventAttendees() returns paginated list
- ✅ searchAttendees() filters by name
- ✅ searchAttendees() filters by email
- ✅ addAttendee() creates new attendee
- ✅ removeAttendee() removes attendee
- ✅ RLS prevents non-admin access
- ✅ API returns 403 for non-admin

**Test file:** `tests/attendees.test.ts`

---

#### 0.2.2.9 Integration Tests ✅

**Status:** PENDING

**Tests:**
- ✅ Admin adds attendee → Attendee appears in list
- ✅ Admin searches attendees → Returns correct results
- ✅ Non-admin tries to list attendees → 403 Forbidden
- ✅ Pagination works (page=2, limit=10)

---

#### 0.2.2.10 E2E Tests ✅

**Status:** PENDING

**Test:**
- ✅ Full flow: Admin creates event → Adds 5 attendees → Lists them → Searches → Filters by role

---

## 🔗 Dependencies

**Blockers:**
- ✅ Authentication module (EPIC-0 prerequisite)
- ✅ Events module (EPIC-0 prerequisite)

**Blocked by:**
- None

**Enables:**
- Story 1.2 (Select attendees for proposal send)

---

## 📊 File List

**Backend (Phase 1-2):**
- [ ] `src/db/migrations/001-create-event-attendees.sql` — Migration
- [ ] `src/types/attendee.ts` — TypeScript types
- [ ] `src/lib/attendees.ts` — Attendees service
- [ ] `src/routes/attendees.ts` — API endpoints
- [ ] `src/controllers/attendeeController.ts` — Business logic
- [ ] `tests/attendees.test.ts` — Unit tests

**Database:**
- [ ] `.aios-core/data/schema-updates.md` — Schema documentation (updated)

---

## 🔒 Security Considerations

- **RLS:** Only event admin can view attendee list
- **Email Validation:** Validate email format before storing
- **Duplicate Prevention:** Unique constraint on (event_id, email)
- **SQL Injection:** Use parameterized queries (Supabase client)
- **Authorization:** Verify user is event admin before returning list

---

## 🎯 Quality Gates (CodeRabbit)

**Pre-Development:**
- [ ] Database schema reviewed
- [ ] API design reviewed
- [ ] RLS policies designed

**Pre-Merge:**
- [ ] All tests passing
- [ ] Code coverage > 80%
- [ ] RLS policies verified
- [ ] Pagination works correctly
- [ ] Linting passes
- [ ] CodeRabbit approval

---

## 📝 Notes for Dex (@dev)

- Attendees can be email-based (for invitations) or user_id-based (registered users)
- Design for future: Support inviting via email (pending → accepted flow)
- Keep API simple: List, search, add, remove operations
- RLS is critical: Only admins should see attendee lists
- Consider: Bulk import attendees (CSV) — Future enhancement
- Coordinate with Story 1.2 for RecipientSelector component design

---

## ✨ Success Criteria Summary

When this story is DONE:
1. ✅ Database schema created and migrated
2. ✅ Attendees service implemented
3. ✅ API endpoints working (GET, POST, DELETE)
4. ✅ Search and pagination working
5. ✅ RLS policies enforced
6. ✅ All tests passing
7. ✅ Ready for Story 1.2 RecipientSelector
8. ✅ CodeRabbit approves

---

---

## 📊 Dev Agent Record

**Agent:** @dev (Dex)
**Status:** PENDING
**Assigned:** 2026-03-03
**Started:** —
**Completed:** —

### Implementation Progress

| Phase | Step | Status | Tests | Notes |
|-------|------|--------|-------|-------|
| 1 | 0.2.2.1 Migration | ⏳ PENDING | — | — |
| 1 | 0.2.2.2 Types | ⏳ PENDING | — | — |
| 2 | 0.2.2.3 Service | ⏳ PENDING | — | — |
| 2 | 0.2.2.4 GET /attendees | ⏳ PENDING | — | — |
| 2 | 0.2.2.5 GET /attendees/:id | ⏳ PENDING | — | — |
| 2 | 0.2.2.6 POST /attendees | ⏳ PENDING | — | — |
| 3 | 0.2.2.7 Frontend Component | ⏳ DEFERRED | — | Optional for MVP |
| 4 | 0.2.2.8-10 Tests | ⏳ PENDING | — | — |

### Test Summary

**Total Tests:** 0/0 (pending)

### Debug Log

*No activity yet*

---

**Story Owner:** @dev (Dex)
**Created by:** @sm (River)
**Status:** Draft → Ready for Dev

---

*Formatura SaaS — Story 0.2.2: Event Attendees Module — CREATED*
